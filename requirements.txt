# WSI Embedding Analysis Pipeline - Python Dependencies
# Install: pip install -r requirements.txt

# === REQUIRED (core pipeline) ===
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0
statsmodels>=0.13
matplotlib>=3.5
seaborn>=0.12
h5py>=3.0

# === OPTIONAL (survival analysis) ===
# The pipeline runs without these but skips survival modeling
scikit-survival>=0.17
lifelines>=0.27

# === OPTIONAL (additional classifiers) ===
# Adds XGBoost, LightGBM, CatBoost to the 24-model benchmark
xgboost>=1.6
lightgbm>=3.3
catboost>=1.0
